// 
// Decompiled by Procyon v0.5.36
// 

package lotr.common.world.map;

public enum LOTRMountains
{
    BAG_HILL((float)LOTRWaypoint.HOBBITON.getX(), (float)(LOTRWaypoint.HOBBITON.getY() - 4), 1.0f, 70), 
    AMON_EREB((float)LOTRWaypoint.AMON_EREB.getX(), (float)LOTRWaypoint.AMON_EREB.getY(), 2.0f, 120), 
    BELEGOST((float)(LOTRWaypoint.BELEGOST.getX() - 2), (float)(LOTRWaypoint.BELEGOST.getY() - 2), 5.0f, 300), 
    NOGROD((float)LOTRWaypoint.NOGROD.getX(), (float)(LOTRWaypoint.NOGROD.getY() + 2), 5.0f, 300), 
    MOUNT_RERIR((float)LOTRWaypoint.MOUNT_RERIR.getX(), (float)LOTRWaypoint.MOUNT_RERIR.getY(), 3.0f, 200), 
    MOUNT_DOLMED((float)LOTRWaypoint.MOUNT_DOLMED.getX(), (float)LOTRWaypoint.MOUNT_DOLMED.getY(), 4.0f, 300), 
    BREE((float)(LOTRWaypoint.BREE.getX() + 3), (float)LOTRWaypoint.BREE.getY(), 2.0f, 500), 
    WEATHERTOP((float)LOTRWaypoint.WEATHERTOP.getX(), (float)(LOTRWaypoint.WEATHERTOP.getY() - 1), 2.5f, 65), 
    CARN_DUM((float)LOTRWaypoint.CARN_DUM.getX(), (float)(LOTRWaypoint.CARN_DUM.getY() - 1), 2.5f, 150), 
    MOUNT_GRAM((float)LOTRWaypoint.MOUNT_GRAM.getX(), (float)LOTRWaypoint.MOUNT_GRAM.getY(), 2.5f, 250), 
    MOUNT_GUNDABAD((float)LOTRWaypoint.MOUNT_GUNDABAD.getX(), (float)LOTRWaypoint.MOUNT_GUNDABAD.getY(), 2.5f, 250), 
    MOUNT_CELEBDIL((float)LOTRWaypoint.MOUNT_CELEBDIL.getX(), (float)LOTRWaypoint.MOUNT_CELEBDIL.getY(), 2.5f, 250), 
    MOUNT_CARADHRAS((float)LOTRWaypoint.MOUNT_CARADHRAS.getX(), (float)LOTRWaypoint.MOUNT_CARADHRAS.getY(), 3.0f, 250), 
    MOUNT_FANUIDHOL((float)LOTRWaypoint.MOUNT_FANUIDHOL.getX(), (float)LOTRWaypoint.MOUNT_FANUIDHOL.getY(), 2.5f, 250), 
    MOUNT_METHEDRAS((float)LOTRWaypoint.MOUNT_METHEDRAS.getX(), (float)LOTRWaypoint.MOUNT_METHEDRAS.getY(), 2.5f, 250), 
    EAGLES_EYRIE((float)LOTRWaypoint.EAGLES_EYRIE.getX(), (float)LOTRWaypoint.EAGLES_EYRIE.getY(), 3.5f, 100), 
    EREBOR((float)(LOTRWaypoint.EREBOR.getX() - 1), (float)(LOTRWaypoint.EREBOR.getY() - 4), 5.0f, 300), 
    WEST_PEAK((float)(LOTRWaypoint.WEST_PEAK.getX() - 1), (float)(LOTRWaypoint.WEST_PEAK.getY() - 1), 4.0f, 250), 
    EAST_PEAK((float)(LOTRWaypoint.EAST_PEAK.getX() + 1), (float)(LOTRWaypoint.EAST_PEAK.getY() - 1), 4.0f, 250), 
    CERIN_AMROTH((float)LOTRWaypoint.CERIN_AMROTH.getX(), (float)LOTRWaypoint.CERIN_AMROTH.getY(), 1.0f, 80), 
    CARAS_GALADHON((float)LOTRWaypoint.CARAS_GALADHON.getX(), (float)LOTRWaypoint.CARAS_GALADHON.getY(), 1.5f, 150), 
    DOL_GULDUR((float)LOTRWaypoint.DOL_GULDUR.getX(), (float)LOTRWaypoint.DOL_GULDUR.getY(), 1.5f, 100), 
    TREEBEARD_HILL((float)LOTRWaypoint.TREEBEARD_HILL.getX(), (float)LOTRWaypoint.TREEBEARD_HILL.getY(), 1.0f, 80), 
    AMON_HEN((float)LOTRWaypoint.AMON_HEN.getX(), (float)LOTRWaypoint.AMON_HEN.getY(), 1.5f, 100), 
    AMON_LHAW((float)LOTRWaypoint.AMON_LHAW.getX(), (float)LOTRWaypoint.AMON_LHAW.getY(), 1.5f, 100), 
    EDORAS((float)(LOTRWaypoint.EDORAS.getX() + 1), (float)(LOTRWaypoint.EDORAS.getY() - 1), 1.5f, 140), 
    ALDBURG((float)(LOTRWaypoint.ALDBURG.getX() + 1), (float)(LOTRWaypoint.ALDBURG.getY() - 1), 1.5f, 120), 
    HALIFIRIEN((float)LOTRWaypoint.HALIFIRIEN.getX(), (float)(LOTRWaypoint.HALIFIRIEN.getY() + 1), 2.5f, 170), 
    CALENHAD((float)LOTRWaypoint.CALENHAD.getX(), (float)(LOTRWaypoint.CALENHAD.getY() + 1), 2.0f, 100), 
    MINRIMMON((float)LOTRWaypoint.MINRIMMON.getX(), (float)(LOTRWaypoint.MINRIMMON.getY() + 1), 2.0f, 120), 
    ERELAS((float)LOTRWaypoint.ERELAS.getX(), (float)(LOTRWaypoint.ERELAS.getY() + 1), 1.5f, 100), 
    NARDOL((float)LOTRWaypoint.NARDOL.getX(), (float)(LOTRWaypoint.NARDOL.getY() + 1), 1.5f, 150), 
    EILENACH((float)LOTRWaypoint.EILENACH.getX(), (float)(LOTRWaypoint.EILENACH.getY() + 1), 2.0f, 130), 
    AMON_DIN((float)(LOTRWaypoint.AMON_DIN.getX() + 1), (float)LOTRWaypoint.AMON_DIN.getY(), 2.0f, 120), 
    MINDOLLUIN(LOTRWaypoint.MINAS_TIRITH.getX() - 4.0f, (float)LOTRWaypoint.MINAS_TIRITH.getY(), 1.0f, 200), 
    TIRITH_HILL(LOTRWaypoint.MINAS_TIRITH.getX() - 2.5f, (float)LOTRWaypoint.MINAS_TIRITH.getY(), 3.5f, 170), 
    DOL_AMROTH((float)(LOTRWaypoint.DOL_AMROTH.getX() - 2), (float)LOTRWaypoint.DOL_AMROTH.getY(), 2.2f, 250), 
    ERECH((float)LOTRWaypoint.ERECH.getX(), (float)LOTRWaypoint.ERECH.getY(), 2.5f, 150), 
    MOUNT_DOOM((float)(LOTRWaypoint.MOUNT_DOOM.getX() + 1), (float)(LOTRWaypoint.MOUNT_DOOM.getY() + 2), 5.0f, 250, 25);
    
    private int xCoord;
    private int zCoord;
    private float height;
    private int range;
    private int lavaRange;
    
    private LOTRMountains(final float x, final float z, final float h, final int r) {
        this(x, z, h, r, 0);
    }
    
    private LOTRMountains(final float x, final float z, final float h, final int r, final int l) {
        this.xCoord = LOTRWaypoint.mapToWorldX(x);
        this.zCoord = LOTRWaypoint.mapToWorldZ(z);
        this.height = h;
        this.range = r;
        this.lavaRange = l;
    }
    
    private float getLavaCraterHeight() {
        return (1.0f - this.lavaRange / (float)this.range) * this.height * 0.4f;
    }
    
    public float getHeightBoost(final int x, final int z) {
        final double dx = x - this.xCoord;
        final double dz = z - this.zCoord;
        final double distSq = dx * dx + dz * dz;
        final double rangeSq = this.range * this.range;
        if (distSq < rangeSq) {
            if (this.lavaRange > 0) {
                final double lavaRangeSq = this.lavaRange * this.lavaRange;
                if (distSq < lavaRangeSq) {
                    return this.getLavaCraterHeight();
                }
            }
            final double dist = Math.sqrt(distSq);
            final float f = (float)(dist / this.range);
            return (1.0f - f) * this.height;
        }
        return 0.0f;
    }
    
    public static float getTotalHeightBoost(final int x, final int z) {
        float f = 0.0f;
        for (final LOTRMountains m : values()) {
            f += m.getHeightBoost(x, z);
        }
        return f;
    }
    
    public static boolean mountainAt(final int x, final int z) {
        return getTotalHeightBoost(x, z) > 0.005f;
    }
    
    public static boolean mountainNear(final int x, final int z, final int range) {
        for (final LOTRMountains m : values()) {
            final double mtnRange = range + m.range;
            final double rangeSq = mtnRange * mtnRange;
            final double dx = x - m.xCoord;
            final double dz = z - m.zCoord;
            final double distSq = dx * dx + dz * dz;
            if (distSq <= rangeSq) {
                return true;
            }
        }
        return false;
    }
    
    public static int getLavaHeight(final int x, final int z) {
        for (final LOTRMountains m : values()) {
            if (m.lavaRange > 0) {
                final double dx = x - m.xCoord;
                final double dz = z - m.zCoord;
                final double distSq = dx * dx + dz * dz;
                final double lavaRangeSq = (m.lavaRange + 6) * (m.lavaRange + 6);
                if (distSq < lavaRangeSq) {
                    return Math.round(m.getLavaCraterHeight() * 110.0f);
                }
            }
        }
        return 0;
    }
}
